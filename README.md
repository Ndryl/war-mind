# Projeto Carpa Dourada

Simulador competitivo de barcos de pesca usando IA (BFS/DFS).

## Execução

Crie o ambiente virtual, instale as dependências e execute o `main.py`.

```bash
python -m venv .venv
.\.venv\Scripts\Activate
pip install -r requirements.txt
python main.py
```
